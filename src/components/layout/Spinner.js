import React, {Fragment} from 'react';

import './Spinner.css';

export const Spinner = () => {
    return (
        <Fragment>
            <div className="spinner"></div>
        </Fragment>
    );
};
